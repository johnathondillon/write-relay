{
  "name": "@writerelay/node",
  "version": "0.1.0",
  "private": true,
  "description": "Emit WriteRelay events through an existing PostgreSQL transaction",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsc --noEmit --strict --module NodeNext --target ES2022 test/types.ts && node --test test/*.test.mjs",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "typescript": "~7.0.2"
  }
}

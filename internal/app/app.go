// Package app contains the top-level application composition boundary.
//
// Milestone 1 keeps lifecycle composition in the CLI because there is only one
// capture daemon. Delivery workers will move shared lifecycle wiring here.
package app
